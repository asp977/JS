// ternary operator or conditional operator


let age = 5 ;
let drink = (age >= 2) ? "milk" : "cold drink"; 
//syntax --> condition ? val1 : val2  ;

// Conditional Operator returns value based on the condition. It is like if-else.

console.log(drink);